<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">
    <% if (locals.d) {%>
        <h2>Recipe Name : <%= d.name %></h2>

        <h3>Ingredients:</h3>

    <ul id="ingredientsList">
      <li>
        <%= d.ingredients.protein.name%> , <%= d.ingredients.protein.preparation %>
      </li>
      <li>
        <%= d.ingredients.salsa.name%> , <%= d.ingredients.salsa.spiciness%>
      </li>
     
        <% d.ingredients.toppings.forEach(function(toppings) {%>
          <li> <%= toppings.name %> : <%= toppings.quantity %> ( <%= toppings.ingredients %> ) </li>
         
        <%})%>
    
    </ul>

        <% } else {%>    <!-- If no recipe yet, show: -->
          
          <h2>Pick your favourite taco ingredient 👆</h2>
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
   <% } %>
    


  </div>
</body>

</html>